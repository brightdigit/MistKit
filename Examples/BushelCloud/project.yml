name: BushelCloud
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  BushelCloud:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
