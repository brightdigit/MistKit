name: CelestraCloud
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  CelestraCloud:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
