name: MistKit
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  MistKit:
    path: .
  MistKitExamples:
    path: Examples
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}
